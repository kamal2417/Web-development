import React from 'react'
import {useRef} from 'react'

function UseRef33() {
    let Krishna=useRef()
  return (
    <div>
      <h3 shiva={Krishna}>UseRef is same similar to the Dom topic of Javascript which renders the page{console.log(Krishna)}</h3> 
    </div>
  )
}

export default UseRef33
